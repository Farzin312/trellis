# Trellis core requires no environment variables.
#
# Add only variables owned by the adopting application or an explicitly enabled
# integration. Never place real credentials in this file; keep local secrets in
# an ignored environment file or a secret manager.

# Optional Phoenix endpoint after project-owned instrumentation:
# PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006
